package stellar.database.tables;

import stellar.database.types.Table;

public class Tables {
    public static PlayerEvents playerEvents = new PlayerEvents();
    public static Playtime playtime = new Playtime();
    public static ServerEvents serverEvents = new ServerEvents();
    public static Users users = new Users();
}
